'use strict';
import appPackageInfo from "../../../package.json";

export const nav_version: string = appPackageInfo.version;
export const layer_version: string = "4.5";
export const version_mismatch: boolean = false;
